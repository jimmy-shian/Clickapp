import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jimmyshian.click',
  appName: 'omniclick-designer',
  webDir: 'dist'
};

export default config;
